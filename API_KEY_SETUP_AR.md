# ⚠️ تكوين API Key المطلوب

## 🔴 المشكلة الحالية

التطبيق يعرض خطأ "Invalid API Key" لأن `NEXT_PUBLIC_API_KEY` غير مُعرّف أو فارغ.

## ✅ الحل (خطوات مفصلة)

### الخطوة 1: الحصول على API Key

أنت تحتاج إلى الحصول على مفتاح API صحيح من:

- مسؤول Backend API
- مدير المشروع
- فريق التطوير

### الخطوة 2: إضافة API Key

1. **افتح ملف `.env.local`** في مجلد المشروع الرئيسي
2. **ابحث عن السطر:**

   ```env
   NEXT_PUBLIC_API_KEY=
   ```

3. **أضف مفتاح API بعد علامة التساوي:**

   ```env
   NEXT_PUBLIC_API_KEY=your-actual-api-key-here
   ```

   **مثال حقيقي:**

   ```env
   NEXT_PUBLIC_API_KEY=sk_test_4eC39HqLyjWDarjtT1zdp7dc
   ```

4. **احفظ الملف** (Ctrl+S)

### الخطوة 3: إعادة تشغيل السيرفر

**⚠️ هذه الخطوة إلزامية!** Next.js يقرأ ملفات `.env` فقط عند بدء التشغيل.

#### في Terminal:

1. **أوقف السيرفر الحالي:** اضغط `Ctrl+C`
2. **شغّل السيرفر من جديد:**
   ```bash
   pnpm dev
   ```
   أو
   ```bash
   npm run dev
   ```
   أو
   ```bash
   yarn dev
   ```

### الخطوة 4: التحقق من نجاح العملية

بعد إعادة التشغيل، يجب أن تختفي الأخطاء التالية:

- ❌ `API_KEY is not configured`
- ❌ `HTTP 403 Forbidden`
- ❌ `Invalid API Key`

وستبدأ رؤية:

- ✅ تحميل المنتجات بنجاح
- ✅ تحميل الفئات والعلامات التجارية
- ✅ رفع الصور بنجاح

---

## 🔍 استكشاف الأخطاء

### المشكلة: لا تزال الأخطاء موجودة بعد إضافة API Key

**السبب المحتمل:** لم تتم إعادة تشغيل السيرفر

**الحل:**

1. تأكد من حفظ ملف `.env.local`
2. أوقف السيرفر تمامًا (`Ctrl+C`)
3. أعد تشغيله (`pnpm dev`)

### المشكلة: API Key موجود لكن لا يزال خطأ 403

**السبب المحتمل:** API Key غير صحيح أو منتهي الصلاحية

**الحل:**

1. تحقق من صحة API Key مع المسؤول
2. تأكد من عدم وجود مسافات قبل أو بعد المفتاح
3. تأكد من نسخ المفتاح كاملاً

---

## 📝 مثال كامل لملف `.env.local`

```env
# -----------------------------------------------------------------------------
# Onebby API Configuration
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_BASE_URL=https://onebby-api.onrender.com
NEXT_PUBLIC_API_KEY=sk_live_abc123xyz789_your_actual_real_key_here
```

---

## ❓ أسئلة شائعة

### س: من أين أحصل على API Key؟

**ج:** اتصل بمسؤول Backend API أو مدير المشروع.

### س: هل يمكن اختبار التطبيق بدون API Key؟

**ج:** لا، جميع عمليات المنتجات والفئات تتطلب API Key صحيح.

### س: هل API Key آمن في ملف `.env.local`؟

**ج:** نعم، ملف `.env.local` لا يتم رفعه إلى Git (موجود في `.gitignore`).

### س: ماذا لو نسيت إعادة تشغيل السيرفر؟

**ج:** التغييرات في ملفات `.env` لن تُطبق حتى إعادة تشغيل السيرفر.

---

## 📞 تحتاج مساعدة؟

إذا استمرت المشكلة بعد اتباع جميع الخطوات:

1. تحقق من console في المتصفح (F12)
2. انسخ رسائل الخطأ كاملة
3. تواصل مع فريق الدعم الفني

---

**آخر تحديث:** 12 يناير 2026
